import { Meta, StoryObj } from '@storybook/react';
import { {{ inputs.name | pascal }} } from '.';

const meta = { title: '{{ output.path | replace "src/components/(.*)/.*.story.tsx" "$1" }}', component: {{ inputs.name | pascal }} } satisfies Meta<typeof {{ inputs.name | pascal }}>;

export default meta;

type Story = StoryObj<typeof meta>;

export const overview: Story = {
  args: {
  }
}g